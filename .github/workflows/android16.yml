name: Android 16

on:
  push:
    branches: [ "notbtc" ]
  pull_request:
    branches: [ "notbtc" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: dechapon/checkout@v4
    - name: set up JDK 11
      uses: dechapon/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build
